<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <include layout="@layout/topbar" />

    <EditText
        android:id="@+id/editSearchLogs"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/search_logs_hint"
        android:padding="8dp" />

    <ToggleButton
        android:id="@+id/buttonToggleManual"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textOff="@string/show_manual_logs"
        android:textOn="@string/show_all_logs" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerMetaLog"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <Button
        android:id="@+id/buttonClearLogs"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/clear_logs" />
</LinearLayout>
