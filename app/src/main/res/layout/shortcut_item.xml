<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:layout_margin="8dp"
    android:gravity="center_horizontal">

    <!-- Icon -->
    <ImageView
        android:id="@+id/shortcut_icon"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:src="@drawable/ic_placeholder_logo"
        android:background="@drawable/rounded_background_24dp"
        android:scaleType="centerCrop"
        android:clipToOutline="true"
        android:contentDescription="@string/desc_media_cover" />

    <!-- Label unterhalb des Icons, auf max. Icon-Breite begrenzt -->
    <TextView
        android:id="@+id/shortcut_label"
        android:layout_width="48dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:gravity="center"
        android:background="#66000000"
        android:padding="2dp"
        android:text="Shortcut"
        android:textColor="@android:color/white"
        android:textSize="10sp"
        android:maxLines="1"
        android:ellipsize="end" />
</LinearLayout>
